# Input for user to type something entirely in uppercase
shout_prompt = input("Type something in all caps: ")

# Convert the input test to lowercase
shout_prompt = shout_prompt.lower()

# Prints the lowercase string
print(shout_prompt)